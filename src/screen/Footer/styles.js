import { Platform } from 'react-native';

const styles = {
    container: {
        flex: 8,
        backgroundColor:'transparent',
    },    
    footerStyle:{
        width: 30,
        height: 30,
        marginTop:5,
        alignSelf:'center',
        marginBottom:3
    },
}

export default styles